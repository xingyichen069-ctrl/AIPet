# 六项功能使用说明

人格文件保持原样。新增能力使用独立的本地数据，不需要增加常驻面板。

## 启动

- macOS：双击 `启动桌宠.command`。
- Windows：双击 `启动桌宠.bat`。
- 第一次启动会准备 Python 依赖，后续直接使用项目内的环境。建议 Python 3.12；安装了 uv 时脚本会自动选择。
- 双击角色或通过托盘打开对话；角色右键也有入口。

## 接着上次聊

新的用户消息和回复会保存在 `data/companion.sqlite3`，下次启动默认恢复当前话题。最近对话进入上下文，较早的对话按问题中的关键词补充检索。

聊天右上角 `···` → **另起话题**；旧对话保留，可通过 **以前的话题** 找回。另起话题不会清除长期记忆。历史展示最近 120 条，上下文从当前话题最近 1000 条中选择，避免无限增长。

旧版本没有保存过的完整双方对话无法补回。本次复制时已有的长期记忆仍保留。

## 一句话约定

直接输入：

- 四十分钟后提醒我收衣服
- 一小时二十五分钟后提醒我看烤箱
- 下次我来找你，提醒我发报告

常见相对时长在本地处理，无需 API。其他明确的时间表达通过现有模型调用约定工具处理；模糊时间需要说清楚。

到点只弹一张小气泡：**完成** / **稍后 10 分钟**。右键角色或聊天 `···` → **查看约定**，可完成、延期、取消。

应用需要运行才能提醒。电脑睡眠、应用关闭期间错过的提醒会在恢复运行后补发；下次见面型约定在下一次打开对话时触发。多条到期提醒依次显示，未处理的提醒重启后仍保留。

## 安静陪伴

输入“陪我写半小时”，或右键角色 → **安静陪伴半小时**。角色减少动作，收起思考面板，显示一行倒计时，到点轻轻提示。

“结束陪伴”或右键 **结束陪伴** 可以提前结束。右键 → **高级** → **显示陪伴计时** 可以关闭计时标记。重新打开应用仍按原来的结束时间恢复，不会重新计时。

## 记忆纠正与撤销

右键一条自己的消息：

- **以后记住**：将对应记忆设为长期保留。
- **更正记忆**：替换这条消息及对应记忆，撤回紧随其后的旧回复，避免继续引用旧答案。
- **撤回并忘记**：移除该消息的正文、附件正文、紧随其后的回复以及对应的当前记忆。

也支持“刚才那句别保存”“这件事以后记住”“这条记错了，应该是报告周五交”。这些指向最近一条仍保留的用户消息。对象不明确时使用右键选择。

操作范围是所选消息与对应的当前记忆。历史备份、此前已整理进用户档案的独立事实和其他消息不会被自动改写；本次没有改动人格或用户档案文件。恢复历史备份也会恢复备份当时的数据。

沿用原有隐私词过滤：命中后，本轮双方对话和材料不持久保存。材料和正常聊天会用于现有的模型请求，保存在本机不意味着模型在本机运行。

## 工作状态动作

等待回复、工具处理、正在回答、完成与失败会触发不同的姿态或动作。安静陪伴期间抑制这些动作。不会增加按钮，也不修改人格和临时心理状态。

## 文本材料

把 TXT / Markdown 文件拖进聊天窗口，或者点输入框旁的 `＋`。标签上的 `×` 可在发送前移除。可以同时写一句问题；不写问题直接发送时，默认请求解释材料。

也可直接粘贴文本。Enter 发送，Shift+Enter 换行。每次一个文件，最多 128 KB / 24,000 字；支持 UTF-8、UTF-16 和 GB18030。超限或不支持的文件会明确提示，不会悄悄截断。

材料随该条消息保存，便于重启后继续追问。无需建立资料库。

## 其他

- 回复中可点 **停止**；网络读取最多等待约 30 秒超时，工具若正在执行则等待该次工具返回。停止不会撤销已经执行的约定等操作。
- 失败后可重试同一条消息，不重复保存用户消息；刚创建的相同约定有短时去重。
- 模型、代理、思考档位移至右键 **高级**；托盘可重新打开对话、恢复鼠标点击和退出。
- 备份脚本会把聊天与约定数据库一起保存，使用 SQLite 一致性快照。恢复备份前请退出桌宠。
- 本副本的桌面对话与数据独立于 Downloads 中的原版。若继续使用 Cherry / QQ 共享人格记忆，需要把其现有 AIPet 路径改到这份副本；本次没有自动修改 Cherry 配置。

## 验证

`python -m unittest discover -s tests -v`

测试使用临时数据和模拟回复，不调用真实 API、不改已有记忆。覆盖对话恢复、话题切换、长对话检索、提醒补发/延期/取消、陪伴恢复、消息撤回、隐私过滤、附件、停止和重试。
# 0.2 版：省心更新

- 草稿按话题自动保存在本机，同时保存附件内容和输入位置；发送后清除对应草稿。附件原文件移动或删除也不影响草稿恢复。
- 聊天窗口记住位置、大小、最大化状态和是否打开。正常启动会恢复原状；托盘的“显示桌宠 / 恢复点击”仍可主动找回窗口。
- “··· → 外观”可调整字号和玻璃质感，当场生效并保留偏好。
- 配色与外观规则改动约半秒内自动载入，无需重启；文件临时不完整时保留上一套有效设置。
- 首次安装本版仍需重开桌宠一次。此后一般外观调整无需重开；涉及功能代码的更新重开后会恢复草稿与布局。
