# 小日和项目约定

- 项目工作保留在当前本机目录；原版、旧备份和私人运行资料不得随代码整理删除。
- 除非用户明确要求，不修改人格与边界设定。
- 每次用户要求的功能或修复完成后，更新 `CHANGELOG.md` 和 `VERSION`，检查相关行为，提交代码并建立与版本一致的 Git 标签。
- 日志采用简短的“施工日志”口吻，准确区分已实现、已验证和待验证，不编造发布历史或测试结果。
- 尚有必要验证未完成时使用 beta 版本。检查 Git 差异后再提交，避免把用户的临时设置改动误算作本次功能。
- `.gitignore` 中的密钥、档案、人格、记忆、运行环境和旧备份保留在本机；不强行加入代码提交，也不清理这些文件。
- Git 仓库先保留在本地；没有用户请求时不创建远程仓库、不推送、不上传附件。
- 回退默认在独立目录导出旧版本供比较，不覆盖当前私人数据，不使用破坏性清理命令。
