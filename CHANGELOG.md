# 小日和施工日志

每次更新留一页：版本号、日期、改了什么、验过什么。语气可以皮一点，功能和验证结果要写实。

## 待更新

下一次小修小补，从这里开工。

## 0.4.0-beta.7 · 2026-09-16

**「有没有新版我看得见，装不装你说了算。」**

加了检查更新。桌宠是右键 → 高级 → 检查更新，QQ 上是主人发 `/版本`（或「更新」「/update」，别人发会被拒）。两个入口共用 `src/update.py`。

**它只查，不下载也不覆盖任何文件。** 有新版就报版本号、给一个下载页，剩下是人的事。不做自动更新是有意的：本机这套是拷贝不是 git 检出，「自动更新」只能是把源码 zip 下一遍覆盖过去 —— 覆盖到一半失败、或者新版本自己带着 bug，桌宠当场就坏。拿这个换省一次点击，不划算。

版本号从根目录 `VERSION` 读。这里本来有个已经踩上的坑：`VERSION` 停在 beta.5，README 写着 beta.6，两边对不上而没人发现。拿 README 或 CHANGELOG 当版本源迟早出事，它们本来是给人看的。

比大小按 semver，两个坑各有用例压着：预发布版比同号的正式版小（`0.4.0-beta.6 < 0.4.0`），`beta.10 > beta.9`（按数字比，不按字符串 —— 字符串比会得出 beta.9 更大）。

只在手动点的时候查，平时零网络请求。请求走 `proxy.py` 检测到的代理；SOCKS 那条路 urllib 不认，用 PySocks 临时把全局 socket 换掉、用完立刻换回来 —— 这段代码跑在桌宠进程里，不能留下一地全局状态。

### 验证

- `update.py selftest` 全过，10 组版本比较 + 真连一次 GitHub。
- `test_desktop` 27 项全过、`test_appearance` 通过。
- 菜单实测：四个主题和字号、玻璃质感都没动，「检查更新」在「重新检测代理」下面。
- QQ 指令 9 组：`/版本`「版本」「更新」「检查更新」「/update」「version」都命中；「更新一下记忆」「版本是什么意思」不命中，交回模型；群友发被拒。
- 弹窗两条分支都跑过：有新版时报版本号、点「打开下载页」开的是 tag 页；已最新时只报一句。
- 「有新版本」时 QQ 收到的那句 68 字、不含 URL（QQ 会拒收带 URL 的整条消息）。

## 0.4.0-beta.6 · 2026-09-16

**「接口是你的，不是我的。」**

读图接口去个人化。`vision.py` 原本写死成某个校园部署，现在是个通用的 OpenAI 兼容客户端（`/chat/completions` + `image_url`），本机跑的、云上的、内网部署的都能接。

`data/secrets.json` 键名跟着改，**旧的不再识别**：

```
sjtu_api_key / sjtu_base_url / sjtu_vision_model
    → vision_api_key / vision_base_url / vision_model
```

顺手删了 `vision_model` 那个 `qwen3.8-27b` 的缺省值 —— 没配就是没配。README 补了一节讲怎么填。

修掉 vision 自检里一条永远绿的用例：「目录不会被当成图」原本拿 `src/` 测，没后缀在第一道闸门就被拒，`is_dir` 那条分支根本走不到。

验证：`vision.py selftest` 全过（含真调一次读图）、`local_tools.py` 自检全过。

## 0.4.0-beta.5 · 2026-09-16

**「图我看得见了。」**

### QQ 收图

之前一直收不到，根因不在下载而在更前面：QQ 把图放在 `attachments` 字段里、正文只有一个空格，而 `parse_event` 只取 `content`，`handle` 又把空格判成「空内容」整条扔掉 —— 三道关卡连着把它挡在外面。现在下载、读成文字、拼进 prompt，同时进对话历史。

URL 带 `rkey` 签名会过期，所以是收到就下载，不缓存。

`MAX_TOOL_ROUNDS` 5 → 8。实测撞过：让她看一张图，先猜错目录、列一次目录、再读图，三步就到顶了，话都没说出来。

中转图存 `data/qq_media/`，7 天自动清。清理只扫这一个目录，手动移出去的、`keep_image` 复制到沙箱的都不受影响。

### 看图

`see_image` 走一个带视觉的模型 —— 她自己的脑子（DeepSeek）是纯文本的，图进去只能干看着。它**不受沙箱限制**（图本来就在电脑各处，拖进来的文件没法挑地方），靠另外两道闸门把关：

| 闸门 | 拦什么 |
|---|---|
| 后缀白名单 | `secrets.json` / `SOUL.md` / `journal.jsonl` / `win.ini` 一律拒 |
| 文件头魔数 | 后缀能改、内容改不了 —— 文本改名成 `.png` 也过不去 |

**第二条是实测出来的**：只装后缀检查时，把文本改名成 `.png`，**内容真的发出去了**（服务器报 400 是它解析不了，不是我们拦住了）。

读图是把整份文件 base64 POST 出去，所以才有上面这两道闸门，以及 QQ 侧的工具屏蔽：非主人的工具表里**没有** `see_image`（13 → 12），**在调模型之前**就摘掉 —— 事后拦的话，模型会看见工具、写调用、失败、再试，又慢又吵。

另外两个配套：`keep_image` 把 QQ 中转的图复制进沙箱长期保存（原本她存不了图，`fs_write` 只能写文本）；对话窗开始收图（那个窗口本来就有完整的拖放附件机制，只是 `read_attachment` 只收 txt/md）。**UI 和主题一行没动。**

### 主题：琪露诺

冰蓝与白，六角雪花。取官方设定的两个锚点：蓝色无袖连衣裙（主色）和背后三对六棱柱状翅膀 —— 六棱柱就是六角，所以画雪花不是随便挑的。

`draw_motif` 按尺寸分两档：小于 24px 时砍掉一半侧枝、线加粗、不点花心。18 条线在 16px 的分隔线上会糊成一团 —— 同尺寸下太极/星星/心都还认得出，只有它成了蓝疙瘩。

`tests/test_desktop.py` 里的主题列表改成从 `THEME_NAMES` 动态取。原来硬编码三个，加了 cirno 之后测试照样绿 —— 因为它在测旧的三套。

### 验证

- 端到端：群里发图 → 她回「诶，这次图进来了哦。绿头发、白裙子、闭着眼睛飘着」。
- 视觉实测：造图 → 读出「今天下午三点开会 / Room 305 / 番茄两个」。
- 沙箱 15 组越界用例全挡（含「合法根 + 用 `..` 钻出去」）。
- 魔数闸门：文本改名 `.png` 被拒，真图正常读。

### 已知

- **`tests/test_desktop.py` 里两个滚动条测试是 flaky 的**（`..._jumps_to_latest`、`..._follows_layout_growth`）。offscreen 平台下 `QTest.qWait(50)` 后布局没跟上就断言了。**验证过跟本次改动无关** —— 退回三主题也照样随机挂（5 次挂 2 次）。没动它。

## 0.4.0-beta.4 · 2026-09-16

**「东西发我，我给你收着。」**

新增本地文件读写 `fs_list` / `fs_read` / `fs_write` / `fs_mkdir`。在 QQ 上直接说「把这段存成 xxx.md」就能落盘，不用回到电脑前。

沙箱根 `D:\CXY`（`data/config.json` 的 `tools.fs_root` 可覆盖）。**拦的是代码，不是自觉。** 实测被拒的写法：

| 写法 | 结果 |
|---|---|
| `读取/../../../AIPet/data/secrets.json` | 被拒 |
| `D:/CXY/../AIPet/data/secrets.json` | 被拒（合法根 + 用 `..` 钻出去） |
| `//?/C:/Windows` | 被拒（设备路径） |
| `D:/其他目录/x.txt` | 被拒（带盘符且不在沙箱内） |
| `....//....//Windows` | 收进沙箱（`....` 是合法目录名，没逃出去） |

第二行是真正难防的：路径以合法根开头，靠 `..` 往外钻，必须先 `resolve()` 再验归属才拦得住。

带盘符的绝对路径不在沙箱内时**明确拒绝**，不剥成相对路径 —— 否则「存到 D:\其他目录」会悄悄变成「存到 D:\CXY\其他目录」，用户按自己说的路径去找什么都找不到。

`mood` 加了 `log` 入口。SOUL.md 那条「每轮回完记一笔」原本**没有配套工具**，她就自己拼了段 JSON 用 `fs_write` 写进沙箱里的 `data\` —— 而 `mood.py` 真正写的是 `AIPet\data\`，两个文件，字段名也对不上（`time`/`mood` vs `ts`/`key`），等于白记。SOUL.md 那条也改成**不写路径**（写了她会猜错）。

### 验证

- 沙箱 15 组用例：10 个越界全部被拒或安全收进沙箱，5 种该放行的写法解析正确。
- 端到端：模型自己调 `fs_write` 把一段 markdown 存成文件，内容一字不差。
- `local_tools.py` 原有 12 项自检全过。

### 已知

- **QQ 消息里的图片仍然收不到。** `parse_event` 只取 `content` 纯文本，不解析附件。官方图片消息通常带 `<image url="..."/>`，但没实测过真实格式 —— 需要真发一张图才能确认。（下一版推翻：真实格式不是 XML 标签，见 beta.5。）
- 纯文本不受影响（md / txt / 代码）：直接粘贴成文字发过来就能存。

## 0.4.0-beta.3 · 2026-09-16

**「一句话的事，不用跑回电脑前面。」**

QQ 指令口。主人在 QQ 上发 `/档位 deep`（或「档位 深究」「level max」「／thinking 省电」）就能远程切思考强度；不带参数则报当前档位和全部可选值。**只认主人** —— 档位开了公共口子就成了公共设施。

`mood.write_log()`：每轮回完往 `data/mood_log.jsonl` 追加一行（时间、停在哪个心理点、由头、触发的那句话）。**没停在任何状态也写**（`key` 为 null），只记「换状态」看不出密度，事后翻不出「为什么这个月老在走神」。

### 修复

`qq_bridge.py` 的自检收尾不再删真实记忆。旧写法是

```python
kept = [e for e in M.load_journal() if e.get("source") != "qq"]
M.save_journal(kept)
```

两个问题叠在一起：真实 QQ 对话的记忆 `source` 也是 `"qq"`，一起被删了；而 `M.load_journal()` 本身还会滤掉用户「撤回并忘记」的条目，拿它的结果去 `save_journal`，等于把那些条目从磁盘上永久抹掉。**实测丢过 4 条。** 改成原样快照 + 原样还原，并加了一条守卫断言盯着「原有条目一条都不能少」。

### 说明

- **群聊那条消息本身仍走 daily 档。** 被动回复只有 5 分钟窗口，切到 deep/max 会稳定吃 `40034128`。所以群里的指令改的是全局档位，对它自己那条消息不生效，单聊和桌宠才生效 —— 回复里会带一句说明，免得调完以为坏了。
- **指令认不出来时交回模型**，不自作主张回「没这个档位」。实测过：「档位是什么意思」这种正常提问会被它吃掉，然后答非所问。宁可漏一个打错字的提示。
- 人格文件的改动（`persona/SOUL.md`）不进仓库，见 `.gitignore`。这次的例句调整同理，只在本机生效。

### 验证

指令口 8 组实测：无参数报档、中文切档、英文别名 + 全角斜杠、非法档位返回 `None`、群友被拒且档位不变、普通聊天（含「档位是什么意思」）不被吃掉、单聊不带群提示、mood 日志有状态和无状态两种形态。`mood.py`、`qq_bridge.py` 原有自检全过；测试碰过的 `thinking.json` / `mood.json` / `mood_log.jsonl` 全部备份还原。

## 0.4.0-beta.2 · 2026-09-15

**「先把门开开，不然没人进得来。」**

新增 `准备环境.bat`，一键准备运行环境。依次尝试便携版自带的 `runtime\`、已建好的 `.venv\`、uv、系统 Python；Python 接受 3.10 到 3.14 之间的任意 64 位版本，不再只认 3.12。依赖默认从清华镜像装 —— 直连 pypi.org 实测约 49 kB/s，PySide6 全家桶要下半小时；镜像实测 22 MB/s。

### 修复

bat 里非 ASCII 注释会引起 cmd 解析错位：cmd 按字节偏移读 bat，中文注释行会让它把下一行从中间劈开、把后半截当命令执行。现在注释一律 ASCII，中文只出现在 `echo` 输出里。（试过 UTF-8 BOM，更糟；GBK 能用但在非中文系统上会坏。）

### 变更

README 删掉过时内容和本机状态：「要变成桌宠还需要两步」早已不成立，「哪些功能需要开着 Cherry Studio」那张表在 QQ 改自建后也不再适用，`agent 名`、QQ 信道状态、本机路径一并去掉。补上「仓库里没有的东西」—— `persona/` `memory/` `view/` 和 `data/` 的大部分都被 `.gitignore` 排除，之前没有任何地方说明，下载下来会找不到。另外清掉 `docs/思考强度配置手册.md` 和 `tools/qq_ctl.py` 注释里的本机路径与用户名。

### 验证

- 在 Python 3.14.5 上从零跑通 `准备环境.bat`：13 个依赖装上，`import PySide6, live2d.v3, ddgs` 全过。改之前 `启动桌宠.bat` 的版本断言会直接拒绝 3.13 和 3.14。
- 核实 PySide6 6.11.2（`cp310-abi3`）和 live2d-py 0.7.0.4（cp310–cp314）的 Windows 轮子覆盖 3.10 到 3.14，不需要现场编译。

### 已知

- `启动QQ.bat` 和 `停止QQ.bat` 找 Python 只认 `.venv\`，不认 `runtime\`。便携版只有 runtime，双击这两个脚本会退回系统 Python 然后失败。开机自启不受影响，它走 `qq_ctl.py` 内部的路径探测。
- Windows 实机启动仍待验证，保留 beta 标记。

## 0.4.0-beta.1 · 2026-09-15

**「右键也开结界，星屑和恋心一起住进来。」**

对话窗、桌宠右键菜单、高级子菜单和托盘菜单共用主题与字号设置。保留原版东方主题，新增雾雨魔理沙（黄黑白、星形）和古明地恋（黄绿蓝、闭眼爱心）两套日夜配色。

「外观 → 主题」支持即时切换、单选标记与重启保留；主题文件继续支持自动重载。菜单加了主题徽章、标题、分隔线和勾选样式，弹出菜单用实色背景保证文字清晰。主题设置由桌宠和对话窗共享，窗口销毁时自动断开外观更新，避免重开后访问已销毁的窗口。

### 验证

67 项本地自动检查通过，覆盖菜单键盘选择、子菜单回调、日夜同步、文件重载、无效文件回退、重启保存、草稿与附件保留及文字对比度。已查看三套主题的日间和夜间 Qt 实际渲染截图。

### 已知

Windows 实机与原生托盘显示尚待验证，保留 beta 标记；本次不改 Live2D 角色模型与人格设定。

## 0.3.1-beta.2 · 2026-09-15

**「人走了，话也要跟上。」**

### 修复

- 说话气泡在人物移动时实时跟随，Live2D 与静态形象共用窗口移动处理，不再等拖动结束。
- 气泡按人物所在屏幕定位并限制在可见范围；首次显示后重新对齐，避免原生窗口偏移。
- Windows 完整便携包包含本次修复。

### 验证

- 60 项本地自动检查通过，新增连续移动、隐藏后重现、跨屏边界检查。
- 气泡相关 3 项检查另在 macOS 原生窗口通过；Windows 实机启动仍待验证。

## 0.3.1-beta.1 · 2026-09-15

**「打包行李，去 Windows 那边住几天。」**

### Windows 迁移

- 启动入口优先使用包内 Windows 64 位运行环境，保留源码版的环境安装入口。
- 新增后台启动日志和“启动诊断”入口，失败时给出日志位置。
- Windows 便携包携带运行依赖、原始资料、历史备份、版本历史与现有私人运行数据。
- 苹果原生玻璃效果在 Windows 上使用兼容视觉外观。

### 验证

- 57 项本地自动检查通过；打包时核对 Windows 二进制架构、依赖标签、模型引用和 SQLite 快照完整性。
- 本包在 Mac 上组装，尚未在 Windows 实机运行，保留 beta 标记。

## 0.3.0-beta.1 · 2026-09-15

**「脑袋慢慢转，想好放一小圈弹幕。」**

### 新增

- 等待与生成回复期间，阴阳玉缓慢旋转，约十秒一圈。
- 回复成功完成时，浮现一圈淡淡的弹幕，约 1.2 秒后消失。
- 取消或出错时停止旋转，不播放完成弹幕。
- 安静陪伴期间停止动效；窗口隐藏时暂停计时，重开不补播已经结束的效果。

### 验证

- 56 项自动检查通过，覆盖等待、成功、取消、错误、安静陪伴和隐藏窗口。
- 已在 macOS 原生窗口验证旋转画面变化、完成动效自动停止，并补验 Liquid Glass、标题栏、缩放和普通窗口层级。

## 0.2.0-beta.1 · 2026-09-15

**「重开窗，不重写半句话。」**

### 新增

- 分话题自动保存草稿、输入位置和附件快照；发送后及时清除，切换话题时分别恢复。
- 保存聊天窗口的位置、大小、最大化状态与是否打开，启动时恢复布局。
- 在“··· → 外观”中调整字号与玻璃通透度，立即生效并保留偏好。
- 主题配色文件与附加样式支持自动重载；编辑过程中的无效内容不会覆盖当前有效外观。
- 原有本地备份同时收录草稿、窗口布局与个人外观偏好。

### 验证

- 53 项自动检查通过，覆盖草稿重开恢复、附件原文件删除、不同话题隔离、已发送草稿清理和外观实时更新。
- 额外验证大字号窄窗口排版、最大化恢复；本版仍待用户重开后补充完整原生窗口确认。

## 0.1.0-beta.4 · 2026-09-15

**「从窗框到符札，统一刷回幻想乡的颜色。」**

### 外观修正

- 标题栏增加独立的实色底层，与内部顶部使用同一套配色，避免系统灰与聊天界面脱节。
- 白天沿用暖米白、博丽朱红、符札金；夜晚沿用深靛蓝、朱红和淡金。
- 恢复顶部朱红细线，加强标题与夜间副标题的主题色。
- 原生玻璃增加随昼夜变化的轻微染色，保留背景模糊、半透明气泡与高光边缘。

### 验证

- 47 项自动检查通过，并渲染检查两套界面配色。
- 在无窗口环境验证 AppKit 标题栏底层、实色值和尺寸调用；完整原生窗口的视觉效果仍待重开后确认。

## 0.1.0-beta.3 · 2026-09-15

**「窗户可以透，牌匾还是要有底色。」**

### 外观修正

- 关闭 macOS 标题栏的透明设置，恢复标准标题栏外观并跟随昼夜模式。
- 小日和标题与阴阳玉所在的顶部区域恢复实色底：白天暖米白、夜间深蓝灰。
- 聊天内容区域继续使用玻璃效果。

### 验证

- 检查昼夜顶部实色绘制和下方半透明表面，以及桌面对话相关功能；原生标题栏仍待重启后的实机确认。

## 0.1.0-beta.2 · 2026-09-15

**「玻璃可以换，神社的朱红不能丢。」**

### 外观修正

- 恢复白天的暖米白底、朱红按钮与浅杏色气泡，夜间回到原来的深蓝灰底。
- 恢复消息气泡的朱红侧边、符札色附件和原有文字颜色。
- 保留原生玻璃效果与半透明表面，提高底色覆盖，让配色不再被冷色玻璃冲淡。

### 验证

- 完成昼夜界面渲染检查与相关功能检查；原生透明效果仍沿用 beta 实机观察状态。

## 0.1.0-beta.1 · 2026-09-15

**「神社装上了玻璃，施工队终于开始记账。」**

今天建立本地版本仓库。这是第一次统一留档，此前的改动合并记录，不补造发布历史。

### 对话与陪伴

- 重开桌宠后接续最近对话，也可以另起话题。
- 一句话设定提醒，到点可选完成或稍后。
- 支持限时安静陪伴、记忆纠正与撤销、文本材料附件。
- 等待、查询、回答完成时，角色呈现不同动作。

### 修修补补

- 中文输入法开始拼字时，提示文字及时消失；选字不误发消息。
- 发送后滚动到最新对话，回复增长时持续跟随；上翻查看历史时保持位置。
- 人物置顶；聊天框与思考强度面板使用普通窗口层级。
- 思考强度面板可独立拖动、保存位置。
- 修正 Live2D 参数调用与 macOS 窗口隐藏问题。

### 外观与额度

- 阴阳玉、神社纸垂与浅色弹幕纹样组成东方风格聊天界面。
- 本地时间 07:00–18:00 浅色，其余时间深色，打开期间自动切换。
- 五档输出 token 上限与记忆 token 预算均提高到原来的 10 倍。
- 新增玻璃界面：macOS 26 优先使用原生 Liquid Glass，旧 macOS 使用毛玻璃；其他环境保留可读的渐变外观。

### 验证记录

- 47 项自动检查通过，包括输入法、滚动、记忆、附件、独立拖动和昼夜切换。
- 用户已重启桌宠，并确认聊天框显示正常。
- 原生窗口自动测试仍受环境限制，无法取得屏幕；本版保留 beta 标记，后续补验玻璃效果在输入、缩放和再次打开时的表现。

### 施工备注

「之前靠记性，现在靠版本号。回滚代码也不该把聊天记忆一起倒回去。」
